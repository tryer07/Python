数据库的创建和删除

数据库的创建：

CREATE DATABASE 数据库名;
-- 推荐写法（防止报错，如果存在就不创建）：
CREATE DATABASE IF NOT EXISTS 数据库名;
-- 举例：
CREATE DATABASE IF NOT EXISTS shlg;

显示所有数据库：

SHOW DATABASES;

进入数据库

USE 数据库名;
-- 举例：
USE shlg;
